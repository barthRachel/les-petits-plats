function recipeFactory(data) {
    const {name, servings, ingredients, time, description, appliance, ustensils} = data;

    function getRecipeCardDOM() {
        //card recipe à coder
    }

    return { getRecipeCardDOM }
}